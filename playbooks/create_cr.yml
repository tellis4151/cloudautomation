---
- name: Create ServiceNow Change Request For Server Provisioning
  hosts: localhost
  gather_facts: false

  tasks:
  - name: Create Change Request Provision VMWare VMs
    servicenow.itsm.change_request:
#      instance: 
#        host: "{{ host }}"
#        username: "{{ username }}"
#        password: "{{ password }}"
      type: standard
      state: new
      short_description: Provision New RHEL VM
      description: Provision Server Workflow
      priority: moderate
      category: network
      risk: low
      impact: low
      assignment_group: Network
