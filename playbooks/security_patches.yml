---
- name: Update security patches on RHEL servers
  hosts: rhel_servers
  gather_facts: false
  become: true

  tasks:
  - name: Update security packages 
    dnf:
      name: "*"
      state: latest
      security: true
