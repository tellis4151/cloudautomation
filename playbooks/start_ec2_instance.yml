---
- name: Start EC2 Instance
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
  - name: Start EC2 Instance
    amazon.aws.ec2_instance:
      region: us-east-1
      volumes:
        - device_name: /dev/sdf
      instance_ids: "{{ instance_ids }}"
      state: started
    